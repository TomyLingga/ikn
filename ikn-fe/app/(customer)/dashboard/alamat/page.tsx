import CustomerAddresses from '@/components/customer/CustomerAddresses';

export const metadata = { title: 'Alamat Pengiriman' };

export default function CustomerAddressesPage() {
  return <CustomerAddresses />;
}
